{
  "name": "interview-take-home-tests-1.2",
  "version": "1.0.0",
  "description": "Monorepo of take-home tests for Droplet",
  "author": "Blake Netzeband <blake.netzeband@gmail.com> (https://blakenetzeband.com/)",
  "scripts": {
    "start:spell-check": "pnpm run -F spell-check start",
    "start:intersection": "pnpm run -F intersection start",
    "build": "pnpm run --parallel --if-present build",
    "lint": "pnpm run --parallel --if-present lint",
    "dev": "pnpm run --parallel --if-present dev"
  },
  "devDependencies": {
    "@types/node": "^22.10.7"
  }
}
